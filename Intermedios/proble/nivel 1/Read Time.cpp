#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main() {
    int n, m;
    vector<ll> puntas(n);
    vector<ll> leer(m);
    for(int i = 0;i < n;i++) cin >> puntas[i];
    for(int i = 0;i < m;i++) cin >> leer[i];
    
}